class Solution(object):
    def numWaterBottles(self, numBottles, numExchange):
        """
        :type numBottles: int
        :type numExchange: int
        :rtype: int
        """

        condition = False
        count = numBottles

        while condition:

            if (numBottles - numExchange)




bottles = Solution()
bottles.numWaterBottles(9, 3)
