"""
B. Encapsulation (Data Hiding)
Encapsulation restricts direct access to data to prevent accidental modification. In Python, this is done via naming conventions:

Protected (_): _name (A hint to others: "Please don't touch this outside the class").

Private (__): __name (Triggers "Name Mangling," making it harder to access from outside).

"""


